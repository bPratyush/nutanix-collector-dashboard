# nutanix-collector-dashboard

Published by an MCP Playground multi-MCP workflow (collector -> chart -> filesystem -> everything -> memory -> github).

The dashboard artifacts (chart URLs, knowledge graph, and a report) are committed on the `feature/collector-dashboard` branch and merged via a pull request.
